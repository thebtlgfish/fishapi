version: 1
services:
  - type: web
    name: fishapi
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --bind 0.0.0.0:5000"
    parameters:
      region: oregon
